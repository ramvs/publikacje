<%= bootstrap_form_for(@publication_type) do |f| %>
  
  <%= f.text_field :name , label: "Nazwa" , required: true %>
  <%= f.fields_for :publication_attributes do |a| %>
  	<%= a.select(:name,options_for_select(@attrs,@selected),{label: "Atrybuty"},{multiple: true})%>
  <% end %>  

  <%= f.submit "Utwórz" , class: "btn btn-primary" %>
<% end %>

<script>
$("#publication_type_publication_attributes_name").chosen({
	width:"100%",
	placeholder_text_multiple: "Wybierz atrybuty"
});
</script
<!--  -->