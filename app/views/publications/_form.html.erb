<%= bootstrap_form_for(@publication) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :description %>
 
 <div class="form-group">
 	<%= select_tag(:subtype_id,options_for_select(@types)) %>
 </div>

  <div class="form-group">
  	<%= select_tag(:author_id,options_for_select(@authors),multiple: true) %>
  </div>

  <%= f.submit "Create Publication" %>
<% end %>
<!--  -->
<script>
$("#author_id").chosen({
	width:"100%",
	placeholder_text_multiple: "Wybierz autorów"
});
$("#subtype_id").chosen({
	width:"100%"
})

</script>